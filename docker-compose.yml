services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-horizon}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-horizonpassword}
      POSTGRES_DB: ${POSTGRES_DB:-horizon_db}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - path: .env
        required: false
    environment:
      NODE_ENV: development
      PORT: 3000
      FRONTEND_URL: http://localhost:${WEB_PORT:-5173}
      DATABASE_URL: postgresql://${POSTGRES_USER:-horizon}:${POSTGRES_PASSWORD:-horizonpassword}@postgres:5432/${POSTGRES_DB:-horizon_db}?schema=public
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-}
      SMTP_SECURE: ${SMTP_SECURE:-false}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASS: ${SMTP_PASS:-}
      MAIL_FROM: ${MAIL_FROM:-}
      SMTP_FROM: ${SMTP_FROM:-}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-contact@academie-horizon.test}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-change-me-now}
      SEED_DEMO_DATA: ${SEED_DEMO_DATA:-false}
      SYNC_ADMIN_PASSWORD_ON_SEED: ${SYNC_ADMIN_PASSWORD_ON_SEED:-true}
      CHOKIDAR_USEPOLLING: "true"
    ports:
      - "${API_PORT:-3000}:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: >
      sh -c "npx prisma migrate deploy
      && npx prisma generate
      && npm run dev -w apps/api"

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    depends_on:
      - api
    environment:
      NODE_ENV: development
      VITE_PORT: ${WEB_PORT:-5173}
      VITE_HMR_CLIENT_PORT: ${WEB_PORT:-5173}
      VITE_PROXY_TARGET: http://api:3000
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    ports:
      - "${WEB_PORT:-5173}:5173"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: npm run dev -w apps/web -- --host 0.0.0.0 --port 5173

volumes:
  postgres_data:
  node_modules:
